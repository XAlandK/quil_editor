# quil_editor
An advanced Quill-based rich text editor that supports both RTL (Right-To-Left) and LTR (Left-To-Right) text directions. Includes live HTML code and Delta format preview, as well as image embedding by URL — all in a single self-contained HTML file.
